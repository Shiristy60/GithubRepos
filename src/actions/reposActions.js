/*import {
    GET_REPOS_REQUEST,
    GET_REPOS_SUCCESS,
    GET_REPOS_FAIL
} from '../constants/reposConstants.js'

export const getRepos = (githubId, clientId, clientSecret, count, sort, repos) => async (dispatch) => {
    try {
        dispatch({
            type: GET_REPOS_REQUEST
        })


        dispatch({
            type: GET_REPOS_SUCCESS
        })

    } catch (error) {
        dispatch({
            type: GET_REPOS_FAIL
        })

    }
}*/